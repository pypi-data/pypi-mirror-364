[build-system]
requires = [
    "setuptools>=68.0.0",
    "wheel",
]
build-backend = "setuptools.build_meta"
[project]
name = "melo-mcp-weather-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.12.0",
]
[project.scripts]
melo-mcp-weather-demo = "weather:main"
[tool.setuptools]
packages = ["src"]