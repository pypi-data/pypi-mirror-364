[tool.poetry]
name = "delphy"
version = "0.0.0"
description = "DEep Learning for PHYlodynamics"
authors = ["Gabriele Marino <gabmarino.8601@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"

[tool.pyright]
include = [
    "phylogenie",
]
typeCheckingMode = "strict"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
