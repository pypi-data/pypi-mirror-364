[project]
name = "terraformreviewer"
version = "0.1.2"
description = "Generate Terraform READMEs and inline comments using Claude"
authors = [{ name = "Your Name", email = "you@example.com" }]
dependencies = ["anthropic", "typer"]
readme = "PROJECT_DESCRIPTION.md"

[project.scripts]
terraformreviewer = "terraformreviewer.cli:app"

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"
