services:
  main:
    build:
      context: .
      x-bake:
        args:
          - --attest=type=sbom
      dockerfile: Dockerfile
    image: deanlight/stringdale-ci:0.3.7.beta
    volumes:
      - ./:/app
    stdin_open: true
    tty: true 
    env_file:
      - .env.dev