[tool.poetry]
name = "barid"
version = "0.1.0"
description = "Fully asynchronous Python client for Barid temporary email API"
authors = ["Hexa"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"
httpx = {extras = ["http2"], version = "*"}
pydantic = "*"
email-validator = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
