[project]

name = "mxmncl-readtime"
version = "0.1.0"
description = "gives an estimate on how long a text takes to read"
authors = [{ name=  "Maxime Nicolai"}]
dependencies = []
readme = "README.md"
requires-python = ">=3.7"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"