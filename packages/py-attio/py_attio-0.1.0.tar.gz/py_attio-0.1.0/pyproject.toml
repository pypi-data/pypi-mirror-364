[project]
name = "py-attio"
version = "0.1.0"
description = "A Python wrapper for the Attio API"
authors = [{ name = "Benjamin Hawn", email = "ben@neondeerdata.com" }]
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["requests"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"