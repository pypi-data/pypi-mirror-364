[project]
name = "pyonesend"
version = "1.0.0"
description = "Secure, one-time file/folder transfer with public download links and CLI or importable API."
authors = [{name = "Your Name", email = "your@email.com"}]
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[project.scripts]
pyonesend = "pyonesend.cli:main" 

[project.optional-dependencies]
encryption = ["pyminizip"]
qr = ["qrcode"] 

[tool.setuptools]
packages = ["pyonesend"] 