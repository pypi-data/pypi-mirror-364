[project]
name = "agent-expert-panel"
version = "0.1.2"
description = "A panel of experts that will solve problems and discuss topics"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    {name = "Agent Expert Panel Team"}
]
license = "MIT"
keywords = ["ai", "agents", "expert-panel", "multi-agent", "discussion"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "autogen-agentchat>=0.6.4",
    "autogen-core>=0.6.4",
    "autogen-ext[mcp,openai]>=0.6.4",
    "pydantic>=2.11.7",
    "pydantic-settings[yaml]>=2.10.1",
    "rich>=14.0.0",
    "tiktoken>=0.9.0",
]

[project.urls]
Homepage = "https://github.com/zbloss/agent-expert-panel"
Repository = "https://github.com/zbloss/agent-expert-panel"
Documentation = "https://github.com/zbloss/agent-expert-panel#readme"
Issues = "https://github.com/zbloss/agent-expert-panel/issues"

[project.scripts]
agent-panel = "agent_expert_panel.main:main"
expert-panel = "agent_expert_panel.main:main"

[project.optional-dependencies]
dev = [
    "ruff>=0.12.4",
    "pytest>=8.4.1",
    "pytest-asyncio>=1.1.0",
    "pytest-cov>=6.2.1",
]

[dependency-groups]
dev = [
    "ruff>=0.12.4",
]
test = [
    "pytest>=8.4.1",
    "pytest-asyncio>=1.1.0",
    "pytest-cov>=6.2.1",
    "pytest-mock>=3.12.0",
    "PyYAML>=6.0.1",
]

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["py.typed"]

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
ignore = [
    "E501",  # line too long, handled by black
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = "--cov=agent_expert_panel --cov-report=term-missing"
asyncio_mode = "auto"
markers = [
    "integration: marks tests as integration tests",
    "slow: marks tests as slow running tests",
]
