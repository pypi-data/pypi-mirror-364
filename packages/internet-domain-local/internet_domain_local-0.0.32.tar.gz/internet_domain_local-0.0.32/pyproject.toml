[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.poetry]
name = "internet-domain-local"
version = "0.0.18" # https://pypi.org/project/internet-domain-local
description = "internet-domain-local Python Package"
readme = "README.md"
authors = [
    "Circlez.ai <info@circlez.ai>",
]
