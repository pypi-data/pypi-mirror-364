[project]
name = "django-admin-commander"
version = "0.2.0"
description = "A Django app to run management commands from the admin panel"
readme = "README.md"
authors = [
    { name = "Lucas Camillo" }
]
requires-python = ">=3.11"
dependencies = [
    "django>=4.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
