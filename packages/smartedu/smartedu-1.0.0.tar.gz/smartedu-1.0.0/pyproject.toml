[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "smartedu"
version = "1.0.0"
description = "CLI backend for student/course/enrollment/grades management"
authors = [{name = "Vinay Kumar Dubba"}]
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[tool.setuptools.packages.find]
where = ["smartedu"]
