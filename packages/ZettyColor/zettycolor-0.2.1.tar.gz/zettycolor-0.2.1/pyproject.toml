[project]
name = "ZettyColor"
version = "0.2.1"
description = "Terminal style"
authors = [{ name = "Zetty"}]
license = { text = "MIT" }
readme = "README.md"
requires-python = ">=3.7"
dependencies = []

[build-system]
requires = ["setuptools>=61.0", "wheel", "keyboard"]
build-backend = "setuptools.build_meta"
