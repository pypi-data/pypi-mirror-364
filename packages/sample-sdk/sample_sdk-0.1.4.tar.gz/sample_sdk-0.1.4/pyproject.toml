[project]
name = "sample-sdk"
version = "0.1.4"
description = "This is a sample project"
readme = "README.md"
authors = [
    { name = "swapna", email = "swapna.s@gadgetsoftware.com" }
]
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.1.1",
]

[tool.setuptools.packages.find]
where = ["src"]  # Assumes your code is in a 'src' folder

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
