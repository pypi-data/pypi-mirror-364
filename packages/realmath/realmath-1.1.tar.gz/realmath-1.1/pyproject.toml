[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "realmath"
version = "1.1"
description = "A humanized math library"
authors = [
    { name = "Benyamin Ghaem"}
]
readme = "README.md"
requires-python = ">=3.7"
dependencies = []
