[project]
name = "pcgw_api"
version = "0.2.0"
description = "Client for the PCGamingWiki API"
authors = [
    { name = "Idlusen", email = "boulanger.m@mailoo.org" },
]
dependencies = [
    "httpx>=0.28.1",
]
requires-python = ">=3.13"
readme = "README.md"

[project.license]
text = "LGPL-2.1"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool.pdm]
distribution = true
