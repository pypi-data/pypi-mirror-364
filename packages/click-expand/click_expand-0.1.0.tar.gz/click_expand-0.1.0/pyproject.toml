[build-system]
requires = ["flit_core >=3.11,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "click_expand"
authors = [{name = "jamerfort", email = "jamerfort+pypi@gmail.com"}]
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
dynamic = ["version", "description"]

[project.scripts]
click-expand = "click_expand.__main__:main"
