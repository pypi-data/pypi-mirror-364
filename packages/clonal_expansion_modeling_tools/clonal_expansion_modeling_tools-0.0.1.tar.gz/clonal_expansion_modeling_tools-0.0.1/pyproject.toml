[build-system]
requires = ["flit_core >=3.11,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "clonal_expansion_modeling_tools"
authors = [{name = "Jordan Snyder", email = "jordan@ruc.dk"}]
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
dynamic = ["version", "description"]
dependencies = [
  "numpy",
  "pandas",
  "numba",
  "datetime",
  "scipy",
  "statsmodels"
]