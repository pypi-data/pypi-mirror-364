[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp-bench"
version = "0.0.1"
authors = [
  { name="Xing Han Lù", email="mcp-benchmark@googlegroups.com" },
]
description = "A package to install mcp-benchmark"
readme = "README.md"
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "mcp-benchmark",
]
