[project]
name = "ksei-mcp"
version = "1.0.0"
description = "MCP server for KSEI API access"
dependencies = [
    "mcp>=0.9.0",
    "requests",
    "PyJWT",
    "fake-useragent",
    "python-dotenv"
]

[project.scripts]
ksei-mcp = "ksei_mcp:main"
