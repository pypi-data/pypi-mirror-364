[build-system]
requires = ["uv>=0.1.0", "setuptools>=65.0"]
build-backend = "setuptools.build_meta"

[project]
name = "xiaolihuidaofirstpackage"
version = "0.1.0"
description = "A sample Python package"
authors = [{ name = "xioalihuidao", email = "cd101168@qq.com" }]
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }

[project.scripts]
my-command = "xiaolihuidaofirstpackage.main:greet"

[tool.uv]
packages = [
    { include = "xiaolihuidaofirstpackage", from = "src" }
]