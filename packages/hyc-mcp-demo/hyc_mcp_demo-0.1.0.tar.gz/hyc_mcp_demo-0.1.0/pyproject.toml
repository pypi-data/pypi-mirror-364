[project]
name = "hyc-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.6"
dependencies = [
    "mcp[cli]>=1.12.1",
]

[project.scripts]
hyc-mcp-demo = "hyc_mcp_demo:main"

[build-system]
requires = ["uv_build>=0.8.2,<0.9.0"]
build-backend = "uv_build"
