apiVersion: v1
kind: Service
metadata:
  name: {{ include "iac-gen.fullname" . }}
  labels:
    {{- include "iac-gen.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "iac-gen.selectorLabels" . | nindent 4 }}
