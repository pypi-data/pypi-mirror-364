apiVersion: v1
kind: Service
metadata:
  name: {{ include "appcd-ui.fullname" . }}
  labels:
    {{- include "appcd-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "appcd-ui.selectorLabels" . | nindent 4 }}
