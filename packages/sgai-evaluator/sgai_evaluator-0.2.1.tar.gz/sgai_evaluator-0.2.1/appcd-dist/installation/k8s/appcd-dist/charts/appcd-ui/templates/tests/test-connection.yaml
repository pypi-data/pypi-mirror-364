apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appcd-ui.fullname" . }}-test-connection"
  labels:
    {{- include "appcd-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appcd-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
