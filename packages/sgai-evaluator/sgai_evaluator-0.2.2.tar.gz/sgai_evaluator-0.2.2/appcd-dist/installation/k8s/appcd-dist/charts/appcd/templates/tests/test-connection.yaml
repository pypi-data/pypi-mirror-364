apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "appcd.fullname" . }}-test-connection"
  labels:
    {{- include "appcd.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "appcd.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
