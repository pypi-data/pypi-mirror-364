apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iac-gen.fullname" . }}-test-connection"
  labels:
    {{- include "iac-gen.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iac-gen.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
