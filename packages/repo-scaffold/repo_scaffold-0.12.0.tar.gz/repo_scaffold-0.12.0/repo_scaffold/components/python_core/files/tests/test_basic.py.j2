"""Basic tests for {{cookiecutter.project_slug}}."""

import {{cookiecutter.project_slug.replace('-', '_')}} as package


def test_version():
    """Test that version is defined."""
    assert hasattr(package, "__version__")
    assert package.__version__ == "{{cookiecutter.version}}"


def test_import():
    """Test that the package can be imported."""
    assert package is not None
