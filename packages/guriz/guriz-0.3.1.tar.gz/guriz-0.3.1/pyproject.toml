[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "guriz"
version = "0.3.1"
dependencies = [
    "python-dotenv",
    "requests",
    "fastapi",
    "dbutils",
    "pytz",
    "pymysql",
    "rich",
    "typer",
    "uvicorn"
]

[project.scripts]
guriz = "guriz.cli:main"
