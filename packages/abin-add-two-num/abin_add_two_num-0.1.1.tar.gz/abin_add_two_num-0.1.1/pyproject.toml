[project]
name = "abin-add-two-num"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Abin", email = "trashwbin@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.11.0",
]

[project.scripts]
abin-add-two-num = "abin_add_two_num:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
